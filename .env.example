# Frontend environment variables are bundled into the client.
# Do not place secrets, database credentials, or JWT secrets here.

# Backend root URL. Endpoints remain exactly as documented by the backend,
# for example /auth/signin, /exams/published, /results/session/{id}.
VITE_API_BASE_URL=http://localhost:8085
